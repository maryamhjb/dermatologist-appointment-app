/**
 * Single public shell: max width + horizontal padding match the sticky header and footer
 * at every breakpoint so content edges align.
 */
export const SITE_CONTAINER = 'mx-auto w-full max-w-6xl px-4 sm:px-6'

/** Default vertical rhythm between major blocks on marketing pages (below hero). */
export const SITE_SECTION_Y = 'py-12 md:py-14'

/** Hero outer padding; inner gaps carry most vertical rhythm. */
export const SITE_HERO_Y = 'pt-10 pb-20 sm:pt-12 sm:pb-24 md:pt-16 md:pb-28'
